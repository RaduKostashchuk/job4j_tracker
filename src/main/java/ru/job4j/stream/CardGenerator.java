package ru.job4j.stream;

import java.util.stream.Stream;

public class CardGenerator {/*
    public static void main(String[] args) {
        Stream.of(Suit.values())
                .flatMap(
                        e -> e,
                        e -> Stream.of(Value.values())
                )
                .forEach(System.out::println);
    }*/
}
